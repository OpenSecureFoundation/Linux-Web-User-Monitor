# Monitor-UI (Frontend)

Interface d'administration temps réel développée en Angular.

## Installation
1. Installer les modules : `npm install`
2. Installer Angular CLI (si besoin) : `npm install -g @angular/cli`

## Lancement
`ng serve` (Accessible sur http://localhost:4200)
